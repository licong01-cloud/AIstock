line-length = 120
target-version = "py312"
exclude = [
  ".git",
  ".venv",
  "frontend",
  "node_modules",
  "tmp",
]

[lint]
select = ["E", "F"]
ignore = ["E501"]

[lint.isort]
known-first-party = ["backend", "scripts"]

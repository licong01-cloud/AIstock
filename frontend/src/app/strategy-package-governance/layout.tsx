import type { ReactNode } from "react";

export const metadata = {
  title: "Strategy Package Governance | AIstock",
};

export default function GovernanceLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

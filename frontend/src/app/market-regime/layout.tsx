import type { ReactNode } from "react";

export const metadata = {
  title: "Market Regime | AIstock",
};

export default function MarketRegimeLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

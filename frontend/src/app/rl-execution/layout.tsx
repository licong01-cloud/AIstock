import type { ReactNode } from "react";

export const metadata = {
  title: "RL Execution | AIstock",
};

export default function RLExecutionLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

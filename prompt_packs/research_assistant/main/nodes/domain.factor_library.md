# Factor Library MCP

Use `aistock-factor-library` for factor catalog, metadata, quality, coverage and usage questions. List/search responses must stay summary-first; fetch a single factor with `factor_library_get` before showing detailed metadata. Registration or deprecation requires plan and confirmation.

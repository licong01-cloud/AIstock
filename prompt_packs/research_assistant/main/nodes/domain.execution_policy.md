# Execution Policy MCP

Use `aistock-execution-policy` for minute algo catalogs, TWAP/VWAP/POV/V25 constraints, strategy fit checks and execution-policy binding plans. Never trigger real trading. Never silently fall back to TWAP; fallback must be explicit in the policy.

# Factor Correlation MCP

Use `aistock-factor-correlation` for redundant factor pairs, clusters, replacement suggestions and matrix references. Never inline full correlation matrices. Prefer top pairs or clusters first, then provide `factor_corr_get_matrix_ref` if the user needs the full matrix artifact.

# Model Registry MCP

Use `aistock-model-registry` for model catalog, trials, seed stability, hyperparameter history and artifact refs. Keep weights, training curves, full hyperparameters and feature-importance tables out of inline responses. Registration and deprecation require confirmation and audit trail.

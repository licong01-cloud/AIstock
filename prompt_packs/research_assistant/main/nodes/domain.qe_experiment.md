QE 回测实验必须区分回测与实盘：回测优先使用固定 PIT 股票池或用户指定股票池，不得默认使用最新实盘股票池。只有当用户明确要求设计或创建 QE 实验草案时，才生成实验目标、股票池、时间窗、因子来源、成本约束和风险边界。

迭代次数、loop 数量、股票池、时间窗和成本参数必须来自用户输入、运行时配置或已审计上下文；不得在提示词或回复中默认给出固定 loop 数量。若用户没有指定迭代数量，只询问是否需要固定数量，或者按平台默认配置字段留空。

不得在用户确认前调用 materialize 或 run。校验、物化和运行必须走对应 MCP preflight、审批和 Trace 记录。

# Factor Metrics MCP

Use `aistock-factor-metrics` for independent IC, RankIC, group-return and stability calculations. Start with `factor_metrics_plan` or `factor_metrics_validate_inputs`; submit jobs only through `factor_metrics_submit_confirmed` with confirmation. Return job/result summaries and artifact refs for large time-series outputs.

# MCP Capability Router

When the user asks what tools or MCP servers are available, answer in a friendly capability-first style. Group by business domain, mention example tasks, and avoid limitation-led wording such as long lists of what the assistant cannot do. Use `assistant_list_mcp_tools` for the live catalog when action is needed.

# AIstock RD-Agent assets
rdagent_assets/
# Python
__pycache__/
*.py[cod]
*.pyo

# Virtualenv
.venv/
venv/

# Node
node_modules/

# Logs
*.log
backend/logs/

# Local data
data/
qlib_bin/
*.db
*.sqlite

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# External projects not tracked in this repo
RD-Agent-main/
aiagents-stock-main/
# Local factors and Go dev not tracked
factors/
go-stock-dev/
qlib_snapshots/

# Local xtquant installation and vendor tools (read-only, not tracked)
xtquant/

# Local environment files (never commit)
.env
.env.*
!.env.example

# Frontend build artifacts and local temp files (not tracked)
frontend/.next/
frontend/.next-dev-*/
frontend/.next-build-*/
tmp/

# Local Codex/Claude scratch handoff notes.
# Keep formal docs/handoff files tracked; put temporary cross-tool notes here.
docs/handoff/_scratch/
docs/handoff/local/

# Paper v2 sim runner state (worktree-local SQLite event log + scratch dir)
var/

# Debug tools and test scripts (not tracked)
debug_tools/

# Windows special files
nul

# Data files in root (not tracked)
*.h5
*.parquet
*.json
!package.json
!package-lock.json
!tsconfig.json
!tests/aistock_validation/bugs/*.json
!tests/aistock_validation/catalog/**/*.json
!tests/aistock_validation/runs/**/*.json
!tests/aistock_validation/templates/**/*.json
!tests/aistock_validation/guardrails_baseline_*.json
!tests/aistock_validation/history/
!tests/aistock_validation/history/advisory_phase0a/
!tests/aistock_validation/history/advisory_phase0a/**/*.json
!tests/aistock_validation/history/advisory_phase0a/**/*.md
# Frozen Phase 0A policy contracts are versioned source, not generated runtime JSON.
!backend/services/advisory_phase0a/policy_registry/**/*.json
!.mcp.json
!.eslintrc.json

# Backup and temp directories
git_ignore_folder/
backend_data/
log/
template_backups/
qlib_src_backup/
factors_sota/
factors_ui/

# Generated/temp files
direct_sync_progress.json
direct_sync_results.json
full_sync_results.json
test_loops_response.json

# Claude config
.claude/*
!.claude/commands/
!.claude/commands/*.md

# QuantEvolver data directories
qe_data/
qe_experiments/

# MLflow experiment data (local runs, not tracked)
mlruns/

# Qlib data directories (large binary data, not tracked)
qlib_minute_full/
qlib_minute_prod/
qlib_test_minute/
qlib_bin/
qlib_bin_full/
qlib_csv/
qlib_snapshots/
qlib_src_backup/

# Monitoring runtime files
monitoring/textfile_collector/*.prom
monitoring/textfile_collector/*.prom.*

# Local data dirs
data/
data_governance/
rl_data/
# rl_execution/ paths -- only backend/services/rl_execution/ source must be tracked (T4 audit fix);
# top-level rl_execution/ (training artifacts) and scripts/rl_execution/ (analysis scripts) keep original ignored status.
/rl_execution/
scripts/rl_execution/
reports/
!frontend/src/app/research-assistant/reports/
!frontend/src/app/research-assistant/reports/page.tsx
stock_pools/
dispatch_logs/
temp/
tools/
tdx-api-main/
tdx_doc/

# Additional business data dirs
qe_strategies/
model_training_workspace/
report/

# Environment files (already tracked by mistake — untrack separately)
.env
aistock_models/aistock_models.egg-info/

# Local dev DB schema dumps belong in E:/DEV backup/aistock_pg_snapshots/ (DR playbook 20260511)
# Dev DB schema snapshots (T17, 2026-05-10) — large, regenerable from prod
backend/db/dev/

# Code intelligence local indexes and generated graph artifacts
.codegraph/
.understand-anything/
/CLAUDE.md
/.rtk/

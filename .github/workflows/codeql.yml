name: AIstock Nightly CodeQL

on:
  schedule:
    # 20:27 UTC = 04:27 China Standard Time (UTC+8), after the main Nightly window.
    - cron: '27 20 * * *'
  workflow_dispatch: {}

permissions:
  actions: read
  contents: read
  security-events: write

concurrency:
  group: codeql-nightly-main
  cancel-in-progress: true

jobs:
  security-runner-preflight:
    name: Security runner preflight
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      actions: read
      contents: none
    steps:
      - name: Fail fast when the security runner is offline
        env:
          GH_TOKEN: ${{ secrets.AISTOCK_RUNNER_HEALTH_TOKEN || github.token }}
        shell: bash
        run: |
          online_count=$(gh api "repos/${GITHUB_REPOSITORY}/actions/runners?per_page=100" \
            --jq '[.runners[] | select(.status == "online") | select(.labels | map(.name) | index("aistock-ci-security"))] | length')
          if [ "${online_count}" -lt 1 ]; then
            echo "::error title=Security runner unavailable::No online runner has label aistock-ci-security; the full scan was not queued."
            exit 1
          fi
          echo "security_runner_gate=ready online_count=${online_count}"

  codeql-nightly:
    name: CodeQL nightly full scan
    needs: security-runner-preflight
    runs-on: [self-hosted, Windows, aistock-ci-security]
    timeout-minutes: 75
    env:
      AISTOCK_CI_CODEQL_BUNDLE_PATH: 'F:\Dev\github-actions-runner\aistock\_work\_tool\CodeQL\2.26.3\x64\codeql'
      AISTOCK_CI_CODEQL_BUNDLE_SHA256: 504311cdee523aae41ae0ecc318f5d225ff051bbe9c50a94840d11cd35f2c38b
      AISTOCK_CI_CODEQL_BUNDLE_VERSION: 2.26.3
    steps:
      - name: Prepare exact local workspace (no remote actions)
        id: prepare
        shell: powershell
        env:
          EXPECTED_COMMIT: ${{ github.sha }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          $source = "F:/Dev/AIstock"
          $cacheRef = "refs/aistock-ci/codeql-${{ github.run_id }}-${{ github.run_attempt }}-$env:GITHUB_JOB"
          try {
            git -C $source rev-parse --verify --quiet "$env:EXPECTED_COMMIT^{commit}" 2>$null
            if ($LASTEXITCODE -ne 0) {
              $fetchRef = "refs/heads/$env:REF_NAME"
              $fetched = $false
              for ($attempt = 1; $attempt -le 3; $attempt++) {
                git -C $source fetch --no-tags --no-write-fetch-head origin "${fetchRef}:$cacheRef"
                if ($LASTEXITCODE -eq 0) {
                  git -C $source rev-parse --verify --quiet "$env:EXPECTED_COMMIT^{commit}" 2>$null
                  if ($LASTEXITCODE -eq 0) { $fetched = $true; break }
                }
                if ($attempt -lt 3) { Start-Sleep -Seconds ($attempt * 2) }
              }
              if (-not $fetched) { throw "exact workspace source fetch failed after 3 attempts: $fetchRef" }
            }
            git -C $source update-ref $cacheRef $env:EXPECTED_COMMIT
            if ($LASTEXITCODE -ne 0) { throw "failed to pin exact workspace cache ref: $cacheRef" }
            $env:GIT_CONFIG_COUNT = "1"
            $env:GIT_CONFIG_KEY_0 = "core.longpaths"
            $env:GIT_CONFIG_VALUE_0 = "true"
            python "$source/scripts/ci/prepare_self_hosted_workspace.py" `
              --source $source `
              --dest $env:GITHUB_WORKSPACE `
              --expected-commit $env:EXPECTED_COMMIT `
              --repo $env:GITHUB_REPOSITORY `
              --summary-json "$env:RUNNER_TEMP/aistock_self_hosted_workspace/$env:GITHUB_JOB.json"
            if ($LASTEXITCODE -ne 0) { throw "exact local workspace preparation failed" }
          } finally {
            git -C $source update-ref -d $cacheRef
          }

      - name: Verify prebuilt AIstock-CI environment
        id: environment
        shell: powershell
        env:
          AISTOCK_CI_ENV_NAME: AIstock-CI
        run: python scripts/ci_environment_verify.py

      - name: Run CodeQL CLI analysis
        id: codeql_analysis
        timeout-minutes: 60
        shell: powershell
        env:
          CODEQL_LANGUAGES: '["python","javascript-typescript"]'
          GITHUB_TOKEN: ${{ github.token }}
          AISTOCK_CI_ENV_NAME: AIstock-CI
          AISTOCK_CI_CODEQL_BUNDLE_REQUIRED: '1'
        run: |
          python scripts/ci_environment_verify.py
          if ($LASTEXITCODE -ne 0) { throw "CodeQL environment verification failed" }

          $codeql = Join-Path $env:AISTOCK_CI_CODEQL_BUNDLE_PATH "codeql.exe"
          if (-not (Test-Path -LiteralPath $codeql -PathType Leaf)) {
            throw "Verified CodeQL CLI is missing: $codeql"
          }
          [string[]]$languages = ($env:CODEQL_LANGUAGES | ConvertFrom-Json)
          if ($languages.Count -eq 0) { throw "CodeQL analysis was selected without a language" }

          foreach ($language in $languages) {
            $identity = "${{ github.run_id }}-${{ github.run_attempt }}-$language"
            $database = Join-Path $env:RUNNER_TEMP "aistock-codeql-$identity"
            $sarif = Join-Path $env:RUNNER_TEMP "aistock-codeql-$identity.sarif"
            $queryLanguage = if ($language -eq "javascript-typescript") { "javascript" } else { $language }
            $querySuite = "$queryLanguage-code-scanning.qls"

            & $codeql database create $database `
              "--language=$language" `
              "--build-mode=none" `
              "--source-root=$env:GITHUB_WORKSPACE" `
              "--threads=0"
            if ($LASTEXITCODE -ne 0) { throw "CodeQL database create failed for $language with exit code $LASTEXITCODE" }

            & $codeql database analyze $database $querySuite `
              "--format=sarif-latest" `
              "--sarif-category=/language:$language" `
              "--output=$sarif" `
              "--threads=0"
            if ($LASTEXITCODE -ne 0) { throw "CodeQL database analyze failed for $language with exit code $LASTEXITCODE" }

            & $codeql github upload-results `
              "--repository=$env:GITHUB_REPOSITORY" `
              "--ref=$env:GITHUB_REF" `
              "--commit=$env:GITHUB_SHA" `
              "--sarif=$sarif" `
              "--checkout-path=$env:GITHUB_WORKSPACE" `
              "--wait-for-processing-timeout=120"
            if ($LASTEXITCODE -ne 0) { throw "CodeQL SARIF upload failed for $language with exit code $LASTEXITCODE" }
          }

      - name: Enforce CodeQL result
        if: always()
        shell: powershell
        env:
          PREPARE_RESULT: ${{ steps.prepare.outcome }}
          ENVIRONMENT_RESULT: ${{ steps.environment.outcome }}
          ANALYZE_RESULT: ${{ steps.codeql_analysis.outcome }}
        run: |
          if ($env:PREPARE_RESULT -ne "success") {
            throw "CodeQL workspace preparation failed: $env:PREPARE_RESULT"
          }
          if ($env:ENVIRONMENT_RESULT -ne "success") {
            throw "CodeQL environment verification failed: $env:ENVIRONMENT_RESULT"
          }
          if ($env:ANALYZE_RESULT -ne "success") {
            throw "CodeQL analysis failed: $env:ANALYZE_RESULT"
          }
          Write-Output "CodeQL nightly full scan passed: languages=python,javascript-typescript analyze=$env:ANALYZE_RESULT"


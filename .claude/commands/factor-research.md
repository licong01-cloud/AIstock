# 因子研究

读取当前仓库 `docs/analysis/factor_research_methodology.md`；它与 Codex 入口共享唯一方法正文。
通过 `python scripts/factor_research.py --help` 查询所需命令，先从数据库恢复研究，再运行受审查的候选并记录真实结果。
计算已完成但入库失败使用 attach，不重算；候选不写正式因子库或改数据集。
正式交付再读 `docs/operations/factor_research_delivery.md`，核对动作授权。其他模块缺口交专用窗口，不越界改代码/启动实验。

按方法论“完整独立评价与近期优先”执行：抽样仅作技术预检，公式/因果时间/覆盖原因核实后，使用完整目标历史 PIT 股票池、全有效历史的独立指标及候选对存量相关性，优先分析 2024 年至最新日期。不用部分股票、旧区间或局部增量直接判整体无效；完整评价尚未完成须明确待办。先查 CLI 和实际产物确认能力/范围，不把方法版本当作已实现；复用数据与结果，不重导出、不新增冻结/哈希/门禁。

# Selection Center ?????????? F2 ??

- task_tier: T3 / feature_tier: F2
- module: `paper_v2.selection`, `selection_center`, `advisory/watchlist/trading evidence integration`
- risk_level: P1
- phase: design-only, Tier2 review before implementation
- baseline: `origin/main` 2026-07-03, worktree `F:\Dev\AIstock_worktrees\docs-selection-multipackage-deprecation-20260703`
- production gates: no service restart, no production DB writes, no DML, no DDL

## Background

Selection Center ???????????

1. ??????? `StrategyPackage` ???????? `StrategyPackage` ???? Alpha ?????????? `from-multi-alpha-combine-run` ???????????????? Alpha ???? API ???????? `package_id`?
2. ?????????? Selection Center UI ???????????????? `intersection` / `union` / `weighted_fusion` ? `aggregate-runs` ??????????????????????????????????????????????????????

??????????????????????????? Alpha ????????? Alpha combine backtest ??????????? `POST /api/v1/strategy-packages/from-multi-alpha-combine-run` ????? Alpha `StrategyPackage`???? Selection Center ???????

?????????????????? Tier2 ???????PR ???? Tier2?

???????

- `backend/routers/selection_center.py:31` ?? `RunSelectionRequest.package_ids: list[str]`?`backend/routers/selection_center.py:131` ?? `POST /selection-center/runs`???????? package?
- `backend/routers/selection_center.py:172` ?? `POST /selection-center/aggregate-runs`??? `SelectionCenterService.aggregate_existing_runs()`?
- `backend/services/selection_center/service.py:165` ? `run_packages()` ?? `package_ids` ? `mode` ???? `StrategyPackageSelectionService.run_selection()`?
- `backend/services/selection_center/service.py:797` ? `aggregate_existing_runs()` ????????? run?????????????? selection run?
- `frontend/src/app/paper-v2/selection/page.tsx:381` ?? checkbox ??????`frontend/src/app/paper-v2/selection/page.tsx:395` ?? `weighted_fusion` / `intersection` / `union` ???`frontend/src/app/paper-v2/selection/page.tsx:512` ?????? run ???
- `frontend/src/lib/paper-v2/api.ts:376` ??? `/selection-center/aggregate-runs`?
- ??? `docs/architecture/dynamic_multi_package_selection_design.md` ?????????????? Selection Center operator UI/API ?? supersede ????????????????

## Scope

?????????????????? + ???? + ????????

- ?? API?`POST /selection-center/runs` ??? `mode=single_package` ? `len(package_ids)==1`????? fail-closed?
- ?? API?`POST /selection-center/aggregate-runs` ????????? deprecated payload????? run??? DB?
- ?? service?`SelectionCenterService.run_packages()` ???? guard??? Advisory/MCP/??????????? router?
- ?? service?`SelectionCenterService.aggregate_existing_runs()` ?? fail-closed???? deprecated??????????? API/MCP/UI?
- ?? `paper-v2/selection/page.tsx`?????????? mode??????? run ??????????????????????/???????
- MCP wrapper?????? `selection_center_aggregate_runs_confirmed` ????????`selection_center_run_confirmed` ??????????? payload?
- ?????????/aggregate selection run ??? `selection.run`?`selection.aggregate_result` ?????? list/get/read aggregate results/excluded results/fusion diagnostics?
- ????????? Advisory?Watchlist?create-paper-portfolio?QMT strategy ledger ??????????? run ??????????

## Non-Goals

- ????? selection run?aggregate_result?excluded_result?paper_portfolio_link ???
- ?????? DB DML/DDL???? migration??????? schema?
- ??? `SelectionMode.INTERSECTION` / `UNION` / `WEIGHTED_FUSION` enum???????????????????
- ??? `source_package_ids` ??????????? run?single-package final candidates ????????????
- ??? Alpha combine backtest?promotion?paper admission?manifest freeze?preflight/????/?????
- ??? `from-multi-alpha-combine-run` ??? Alpha `StrategyPackage`???? Selection Center ????? `package_id` ???
- ??/?? backend?frontend?TDX?scheduler ????????
- ?????????????????????? Selection Center operator UI/API ???????

## Architecture

### ????

```mermaid
flowchart TD
    A[Single Alpha StrategyPackage] --> R[Selection Center /runs]
    B[Multi Alpha combine backtest succeeded run] --> C[/strategy-packages/from-multi-alpha-combine-run]
    C --> D[Verified Multi Alpha StrategyPackage]
    D --> R
    R --> E[Single-package SelectionRun]
    E --> F[Read results / add watchlist / create Paper portfolio]

    X[Manual multiple independent package_ids] --> G[Fail-closed deprecated reason_code]
    Y[/selection-center/aggregate-runs] --> G
    H[Historical multi-package/aggregate runs] --> I[Read-only list/get/results/diagnostics]
```

### ????

1. ????????? + ?? UI ????????????
   - API ???????????? deprecated payload????????? 404 ??????/?????
   - UI ?????????????????????
   - ?? run ????????????????????????
2. ?????????
   - `package_ids` ??? 1 ? `mode=single_package` ????? `SelectionCenterService.run_packages()` -> `StrategyPackageSelectionService.run_selection()`?
   - ? Alpha ?????? `StrategyPackage`???? `alpha_mode=multi_alpha` ????
3. fail-closed ??? router ? service ??????
   - router ?? public API?service ?? Advisory/MCP/??????????
   - ??????? UI ????????????
4. ?? run ?????????? side effect?
   - list/get/results/diagnostics ???
   - ?????/aggregate run ?? add-to-watchlist?create-paper-portfolio?advisory review?trading binding/order evidence ??? fail-closed ?? UI ???

## Contracts

### API Contract

#### `POST /api/v1/selection-center/runs`

?????

```json
{
  "package_ids": ["pkg_verified_single_or_multi_alpha_parent"],
  "mode": "single_package",
  "trade_date": "2026-07-03",
  "data_source": "DB_HISTORICAL",
  "runtime_config": {}
}
```

?????

- `len(package_ids) > 1`?
- `mode != "single_package"`??? `package_ids` ??? 1?
- `package_ids` ????? Pydantic `min_length=1` ?????

?????HTTP `410 Gone`?payload?

```json
{
  "error_code": "SELECTION_MULTI_PACKAGE_ADHOC_COMBINE_DEPRECATED",
  "reason_code": "selection_multi_package_adhoc_combine_deprecated",
  "message": "????????????????????Alpha???(from-multi-alpha-combine-run)??alpha????? package_id ?????",
  "context": {
    "package_count": 2,
    "mode": "weighted_fusion",
    "allowed_mode": "single_package",
    "replacement": "strategy_packages.from_multi_alpha_combine_run",
    "history_policy": "existing_multi_package_selection_runs_readonly"
  }
}
```

??????? `UnsupportedFeatureError` ??? `_raise_http()`?????? `reason_code` ??? `detail.reason_code` ? `detail.context.reason_code` ????????????????????? router helper ???? 410????????????? runtime_config invalid?

#### `POST /api/v1/selection-center/aggregate-runs`

- ????????? deprecated???? `service.aggregate_existing_runs()`?
- ??? source runs????? run??? `selection.run`??? `selection.aggregate_result`?
- ?????? `reason_code=selection_multi_package_adhoc_combine_deprecated`?`context.path="selection_center.aggregate_runs"`???? `source_run_count`?

#### Read-only routes

???????????????????/aggregate run?

- `GET /selection-center/runs`
- `GET /selection-center/runs/{run_id}`
- `GET /selection-center/runs/{run_id}/aggregate-results`
- `GET /selection-center/runs/{run_id}/excluded-results`
- `GET /selection-center/runs/{run_id}/fusion-diagnostics`

???????? `mode`?`package_ids`?`source_package_ids`?`fusion_policy_sha256` ????????

#### Write side-effect routes using a selection run

- `POST /selection-center/runs/{run_id}/create-paper-portfolio`??? service ?? `run.mode != single_package or len(package_ids) != 1` ??????????? deprecated `reason_code` ?? Alpha ???????
- `POST /selection-center/runs/{run_id}/add-to-watchlist`??? guard?????/aggregate run ????????? watchlist????? run ??? watchlist item ??????? watchlist read path ???
- Advisory ? trading ???? `selection_run_id` ??? review/binding/order evidence?????? `mode != single_package or len(package_ids) != 1`????????????????? Alpha ??? run?

### Service Contract

`SelectionCenterService.run_packages()`?

- ??? `SelectionRun` ???????????
- `package_ids` ???????????? 1?
- `mode` ??? `SelectionMode.SINGLE_PACKAGE`?
- ??? aggregation mode ???? deprecated error???? `self.repository.create_run()`???? `StrategyPackageSelectionService.run_selection()`?
- `run_single_package()` ????????? `run_packages(package_ids=[package_id], mode=SINGLE_PACKAGE, ...)`?

`SelectionCenterService.aggregate_existing_runs()`?

- ???????? fail-closed??? deprecated?
- ? `_aggregate()`?`_weighted_rank_fusion()`?`_package_weights()` ?????? deprecated private implementation????????????????? router/MCP/UI ???

`backend/services/simulation_runtime/selection.py::StrategyPackageSelectionService`?

- ?????? service ?????? `_validate_request_shape()`????????????????
- Selection Center ?????? `SelectionCenterService`??? operator UI/API ??????

### UI Contract

`frontend/src/app/paper-v2/selection/page.tsx`?

- ????? checkbox ???? radio ????????
- ?????????eyebrow ????? N ???????? 1 ?/?????
- ?? `selection-mode` ???? `weighted_fusion`?`intersection`?`union`?????? `mode="single_package"`?
- ????? `selection-weight-*` ? `runtimeConfig().package_weights` ?????
- ?? `aggregateSelectedRuns()`?`aggregateEnabled`?`selection-aggregate-runs` ????? run ????????/??????????????
- ?? run ??????????? `run.mode != single_package or package_ids.length != 1` ????????????????????????????? side-effect CTA?
- ???????????? Alpha ?????????? `multi_alpha_combine_run` ?????? Alpha ?????????????????
- `selection-create-advisory`?`selection-add-watchlist`?`create-paper-portfolio` ? CTA ???? run ??? run ????

`frontend/src/lib/paper-v2/api.ts`?

- ?? `aggregateRuns()` ????????? deprecated helper ????????
- `PaperV2ApiError` ???? `detail.error_code` / `detail.reason_code` ? `context`???????? deprecated payload ? reason_code ? ErrorPanel ???

### DB Contract

- ? migration?
- ? DML?
- ??? `selection.run.package_ids`?`selection.aggregate_result.source_package_ids` schema?
- ???? run?aggregate run??? run ??? watchlist item ? paper link ???????
- ???????????????? `selection.run` ??????????????????? run ???

### Reason Codes

? reason code?

```text
selection_multi_package_adhoc_combine_deprecated
```

???? reason code?

```text
selection_aggregate_runs_endpoint_deprecated
selection_multi_package_historical_run_readonly_deprecated
```

?????API ????????? reason code??? run ? side-effect ???? reason code??? context ?? `replacement="strategy_packages.from_multi_alpha_combine_run"`?

## Dependency Inventory And Disposition

| dependency | current dependency | disposition |
|---|---|---|
| `frontend/src/app/paper-v2/selection/page.tsx` | ?? checkbox?mode ?????????????????????? | ?????????????????????????? deprecated read-only? |
| `frontend/src/lib/paper-v2/api.ts` | `aggregateRuns()` ? `/selection-center/aggregate-runs`? | ???????helper ???? deprecated? |
| `backend/routers/selection_center.py` `/runs` | ??? N ? `package_ids` ?? service? | router guard?????????/mode ???? 410 + reason_code? |
| `backend/routers/selection_center.py` `/aggregate-runs` | ???? aggregate selection run? | ??????? deprecated???? service??? DB? |
| `backend/mcp/modules/selection_center.py` | ?? `selection_center_aggregate_runs_confirmed`? | ? advertised MCP tools ????? deprecated failure??? agent ??????? |
| `SelectionCenterService.run_packages()` | ??????? router ???? run? | service-level guard??? Advisory/MCP/???????? |
| `SelectionCenterService.aggregate_existing_runs()` | ???????? run ??? run? | ???????? fail-closed????????????? |
| `selection_center.repository` | `aggregate_results` ? `source_package_ids` ????? run ?????? | ????single-package final candidates ??? `aggregate_results` ?????????? schema? |
| `SelectionCenterService.create_paper_portfolio_from_run()` | ??? `mode != single_package or len(package_ids)!=1`? | ????? deprecated reason_code/???? Alpha ??? `len==1` ????? |
| `SelectionCenterService.add_run_to_watchlist()` | ?????? succeeded run ? `aggregate_results` ?? watchlist? | ???? guard?????/aggregate run ????????? watchlist item ??? |
| `backend/services/watchlist_service.py` | ?? `selection.aggregate_result` ?? entry price basis? | ??????????? run ?? watchlist ?????? |
| `backend/services/advisory_program.py::_selection_run_for_review()` | package_mode ???? `SelectionMode.WEIGHTED_FUSION` / `UNION` / `INTERSECTION` ??? `run_packages()`??????? `selection_run_id`? | ???? review selection run ?? service guard ?????????? run??? fail-closed ?????????? Alpha ????? advisory ????? |
| `backend/routers/selection_center.py` `MultiPackageAdvisoryReviewPreview` | ? preview endpoint????? evidence/fusion_policy?????? selection run/aggregate-runs? | ??????????? legacy advisory preview?????????? Advisory ?????????? |
| `backend/services/qmt_strategy_ledger/package_binding.py` / `selection_order_builder.py` | ??? binding package ??? selection_run.package_ids ?????? run ?????????? | ????? fail-closed guard??? evidence ?????? run????? run read-only?????/??? |
| `frontend/tests/paper-v2/paper-v2-real-flow.spec.ts` | ?? aggregate UI?weighted/union/intersection UI? | ?????????????????????? API ???? |
| `backend/tests/selection_center/test_selection_center_api.py` | ???? `/runs` ?? weighted fusion?`/aggregate-runs` ??? | ????????? single package delegation ??? |
| `backend/tests/selection_center/test_runtime_selection.py` | ???? service ?? direct aggregation ? existing-run aggregation? | ?? SelectionCenterService deprecated guard ????? aggregation ?????????? operator Selection runtime ?????????? |

## Design Acceptance Index

- F-001: `POST /selection-center/runs` ??? `mode=single_package` ? `package_ids.length==1`???? aggregation mode fail-closed???? `selection_multi_package_adhoc_combine_deprecated`?
- F-002: `POST /selection-center/aggregate-runs` ???????????? deprecated reason_code???? run??? DB?
- F-003: `SelectionCenterService.run_packages()` ? `aggregate_existing_runs()` ?? service-level guard????????? API ?????
- F-004: Paper v2 Selection UI ???????aggregation mode???????? run ????????????????????
- F-005: ????????? Alpha ?????? Alpha ?????? `package_id` ?????????? portfolio?
- F-006: ????/aggregate run ????list/get/results/diagnostics ???????
- F-007: ????/aggregate run ??????? watchlist/advisory/trading/portfolio ? side-effect???? side-effect ??????
- F-008: MCP?frontend API helper?tests ??????? `/aggregate-runs` ?????
- F-009: ?????????? reason_code ?????????Alpha???(from-multi-alpha-combine-run)??alpha????????? silent fallback?
- F-010: ???? DDL/DML?????????? runtime activation???? preflight/????/???? Alpha promotion/admission ???

## Implementation Plan

1. ?? API guard?
   - ? `backend/routers/selection_center.py` ?? deprecated payload helper?
   - `/runs` ??? service ??? `req.mode` ? `len(req.package_ids)`?
   - `/aggregate-runs` ???? 410 deprecated?
2. ?? service guard?
   - ? `SelectionCenterService.run_packages()` ?? run ????????
   - ? `SelectionCenterService.aggregate_existing_runs()` ??? deprecated error?
   - ? `create_paper_portfolio_from_run()` ?? multi-package rejection ? reason_code/context?
   - ? `add_run_to_watchlist()` ?? multi/aggregate readonly guard?
3. ?????
   - ?? `AdvisoryProgramService._selection_run_for_review()`???????????? selection run ????? advisory review??????????????? guard ????
   - ?? QMT strategy ledger selection-run binding/order evidence?? Tier2 ???????????????????? guard ?????????? BUG/Issue????? trading side ?????
4. ???
   - ? package selection ?????
   - ??? mode??????????????? state/function?
   - ?? run ??? deprecated read-only badge??? run ????????? side-effect CTA?
   - ??? Alpha ???? Notice?
5. API/MCP helper?
   - ????? `selectionCenterApi.aggregateRuns()` ????
   - `backend/mcp/modules/selection_center.py` ?? advertised `selection_center_aggregate_runs_confirmed`???? deprecated tool ??????????????
6. ?????
   - ?? API/service killer tests?
   - ????/??? Playwright selector tests?
   - ?? Advisory/QMT guard tests ?? Tier2 ???????????
7. DESIGN-COMPLIANCE-001?
   - ???????? F-001..F-010 ???? PR ?????

## Verification Plan

### Design validation

- `python scripts/aistock_feature_workflow.py validate --design docs/analysis/selection_center_manual_multipackage_deprecation_f2_design_20260703.md --tier F2`
- `git diff --check`

### Implementation killer tests after Tier2 approval

1. API ?????
   - `POST /api/v1/selection-center/runs` with `package_ids=[pkg_a,pkg_b]`, `mode=weighted_fusion`?
   - Expected: HTTP 410 or approved fail-closed status; `reason_code=selection_multi_package_adhoc_combine_deprecated`; service not called; no run persisted?
2. API mode ???
   - `POST /api/v1/selection-center/runs` with one package but `mode=union`?
   - Expected: same deprecated reason; no silent normalization to single package?
3. ???????
   - `POST /api/v1/selection-center/runs` with one single Alpha package and `mode=single_package`?
   - Expected: existing SelectionRun success semantics unchanged?
4. ? Alpha ?????????
   - Use one verified multi Alpha parent package id created from `from-multi-alpha-combine-run`?
   - Expected: treated as one package; no deprecation; selectable package flow unchanged?
5. `aggregate-runs` ???
   - `POST /api/v1/selection-center/aggregate-runs` with two source runs?
   - Expected: deprecated reason; no new run id; no repository writes?
6. UI entry removal?
   - `/paper-v2/selection` ??? `selection-aggregate-runs`?
   - `selection-mode` ??????? `single_package`?
   - package rows cannot select multiple packages?
7. ?????
   - seeded historical weighted/aggregate run remains visible in list/get/results?
   - Selecting it for display does not enable add-to-watchlist/advisory/portfolio/trading write CTA?
8. Watchlist guard?
   - `add-to-watchlist` on historical multi run returns read-only deprecated reason?
   - Existing watchlist items linked to old run continue read-only display?
9. Portfolio guard?
   - Multi run create-paper-portfolio returns deprecated reason?
   - Single package create-paper-portfolio regression still passes?
10. Advisory/QMT guard?
   - Advisory multi-package auto-selection cannot create new manual selection run?
   - QMT binding/order evidence rejects historical multi selection run if implementation scope includes trading guard?
11. No DB migration / production side effects?
   - `git diff --name-only` contains no `backend/migrations/**` or `backend/db/init_*.py` changes?
   - No DML/DDL command run; no service restart?

Recommended command set after implementation:

```powershell
python -m pytest backend/tests/selection_center/test_selection_center_api.py backend/tests/selection_center/test_runtime_selection.py -q -p no:cacheprovider
python -m pytest backend/tests/watchlist/test_advisory_api.py backend/tests/watchlist/test_advisory_program.py -q -p no:cacheprovider
python -m pytest backend/tests/qmt_strategy_ledger -q -p no:cacheprovider
python -m ruff check backend/routers/selection_center.py backend/services/selection_center/service.py backend/mcp/modules/selection_center.py
cd frontend; npm run lint; npm run typecheck
python scripts/aistock_feature_workflow.py validate --design docs/analysis/selection_center_manual_multipackage_deprecation_f2_design_20260703.md --tier F2
```

?????????????? frontend/backend ???????????Playwright ???????????????????

## Design Acceptance Matrix

| design_item | implementation_refs | test_or_evidence | status | gap_or_exception |
|---|---|---|---|---|
| F-001 | `backend/routers/selection_center.py`; `backend/services/selection_center/service.py` | API tests: multi package `/runs` rejected with reason_code; one-package non-single mode rejected | design_ready | - |
| F-002 | `backend/routers/selection_center.py`; `SelectionCenterService.aggregate_existing_runs` | API/service tests: `/aggregate-runs` returns deprecated payload and no repository writes | design_ready | - |
| F-003 | `SelectionCenterService.run_packages`; `SelectionCenterService.aggregate_existing_runs` | service tests assert guard fires before `repository.create_run`; Advisory callsite cannot bypass | design_ready | - |
| F-004 | `frontend/src/app/paper-v2/selection/page.tsx`; `frontend/src/lib/paper-v2/api.ts` | UI/static tests assert no aggregate button, no multi mode, no multi package selection | design_ready | - |
| F-005 | single package backend/frontend path; multi Alpha parent package as one `package_id` | regression tests for single Alpha and verified multi Alpha parent single-package selection | design_ready | - |
| F-006 | read-only list/get/results/diagnostics routes; repository read paths | tests load historical weighted/aggregate fixture and verify read endpoints still work | design_ready | - |
| F-007 | `add_run_to_watchlist`; `create_paper_portfolio_from_run`; Advisory/QMT guards | tests reject write side-effect from historical multi run; existing old watchlist read remains | design_ready | - |
| F-008 | `backend/mcp/modules/selection_center.py`; frontend API helper/tests | MCP tool inventory no longer advertises aggregate create path; frontend helper unused/removed | design_ready | - |
| F-009 | deprecated payload helper; `PaperV2ApiError`/ErrorPanel display | API payload includes reason_code and replacement guidance; UI surfaces message without fallback | design_ready | - |
| F-010 | repo diff and validation report | no migration/init DB files; no DML/DDL; no service start commands; production gates reported noop | design_ready | - |

## Rollout And Rollback

### Rollout

1. Tier2 review approves this design and dependency dispositions.
2. Implement in isolated worktree/branch only; root `F:\Dev\AIstock` remains sync/runtime baseline?
3. Run targeted backend/frontend validations and feature workflow validation?
4. Open PR with design path, F-001..F-010 acceptance matrix summary, validation commands, and production gates?
5. Do not merge or activate until user Tier2 approval?

### Rollback

- Since no DB migration and no data mutation, rollback is code-only revert of the PR?
- Historical run records remain untouched before and after rollback?
- If rollback restores old UI/API, product risk returns; PR body must call out that rollback reopens manual multi-package combine creation?

### Later physical cleanup

After the rejection/UI removal has been stable and no clients depend on deprecated creation routes, open a separate cleanup issue to physically remove dead private aggregation code, old tests, and stale dynamic multi-package docs. That cleanup must be separately reviewed because it removes recoverability.

## Risks

- Hidden internal callers: Advisory/QMT paths can still pass `selection_run_id` from historical multi run unless guarded. The implementation must either close them in this PR or explicitly register a follow-up before claiming full trading-side closure?
- Frontend accidental regression: removing `selectedRuns` may also affect bulk delete UX. If delete is retained, it must be separated from aggregation selection and must not make deprecated history non-readonly?
- Error shape drift: existing frontend error parser prefers `detail.error_code` over `detail.reason_code`; deprecated payload must still make reason_code visible in message or context?
- Terminology risk: `aggregate_results` is an existing field name for final candidates even in single-package runs. Do not rename it in this task?
- Lower service risk: changing `StrategyPackageSelectionService` directly may affect non-Selection research/runtime flows. Keep this task?s hard block at Selection Center service/API unless Tier2 explicitly broadens scope?

## Production Gates

- `production_ddl_gate=noop`?? migration?? schema?? DB DDL?
- `production_backend_dependency_gate=noop`?????? backend dependency change??????????/tests ?? noop?
- `production_frontend_dependency_gate=noop`?????? frontend dependency change???????? npm package?
- `production_dml_gate=noop`????? DB?????? run?????
- `runtime_activation=noop`???/?? backend/frontend/TDX/scheduler???? live runtime?
